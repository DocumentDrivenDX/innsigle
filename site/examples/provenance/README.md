# Provenance examples (published on microsite)

Generated by `npm run test:provenance-driver` (fixture mode).

| File | Role |
|------|------|
| `human-inputs.json` | Scripted human prompts fed to both agents |
| `session-claude.jsonl` | Journal for Claude agent |
| `session-codex.jsonl` | Journal for Codex agent |
| `session-provenance.json` | L2 merged session provenance |
| `colo.json` | Proposed colophon (L1) |
| `summary.json` | Driver summary |
| `artifacts/` | Markdown files produced in the fixture sessions |

Regenerate:

```bash
node scripts/agent-provenance-driver.mjs --out-dir docs/website/static/examples/provenance
```
